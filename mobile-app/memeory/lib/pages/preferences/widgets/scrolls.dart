import 'package:flutter/material.dart';

class ScrollPreferences extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[
          Text('Выбор оси скрооллинга мемесов!'),
        ],
      ),
    );
  }
}
